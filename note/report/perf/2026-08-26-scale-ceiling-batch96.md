# 批次 96 报告（2026-08-26）— 0.59.0 规模上限探测：40 bot 浸泡（验证轮收口 + 用户终止循环）

主题：去留问询第四次无应答后按默认路径执行 40 bot 规模上限探测；运行中用户下达
**终止循环**决断，本轮以浸泡数据收口，停机竞态扩样不再执行（循环结束，累计 ×9 全
PASS 为最终样本）。无服务器代码变更，版本保持 0.59.0。

## 40 bot × 10 分钟浸泡（种子 papo90，12,000 ticks 连续）

首跑因 vanilla `max-players` 默认 20 在登录期被拒（服务端日志 "The server is full!"，
非服务器缺陷）；harness 增设 `max-players=max(bots,20)` 后重跑。

**门**：logErrors=0、exit 0；窗口全连续。

## 规模曲线终值（同种子 papo90，各 10 分钟）

| 相位 | 10 bot | 20 bot | 40 bot | 4× bot 增量 |
|---|---|---|---|---|
| worlds 稳态 avg/tick | 1167us | 1273us | 1734us | **1.49×（次线性）** |
| connection 稳态 avg/tick | 120us | 125us | 360us | 3×（窗口方差加宽 2.0-9.4%，连接数线性主导） |
| sendChunks 稳态 avg/tick | 42us | 45us | 65us | 1.5× |

40 bot 下主线程 worlds 相位 1.73ms/tick ≈ **3.5% 利用率**——多核调度交付的次线性
扩展到规模上限探测依然成立；connection 随连接数近似线性但绝对值微小（360us/tick）。

## 系列最终状态（批次 78-96，0.51.0 → 0.59.0）

- 优化交付：池预算（吞吐 1.48×）、主线程 IO 清算、join 80→14ms（−82%）、停机窗口
  稳定性加固；红线内已识别面全部做完（批次 92 总收束）。
- 验证矩阵终值：长时（10bot×10min 无上漂）+ 规模（10/20/40bot 次线性曲线）+ 种子
  （跨地形占比稳健）+ 停机竞态（×9 全 PASS）+ 上游 POI 归属闭环。
- **2026-08-26 用户决断：终止循环。0.59.0 为系列终点。**
