# 批次 94 报告（2026-08-26）— 0.59.0 规模放大验证：20 bot 浸泡 + 竞态扩样（回归/验证轮，无代码变更）

主题：去留问询第二次无应答，按默认路径执行规模放大验证轮（批次 93 为 10 bot × 10min，
本轮 **20 bot × 10min**——bot 密度翻倍，检验连接/区块发送/worldgen worker/IO 管线的
争用扩展性）。无服务器代码变更，版本保持 0.59.0。

## 一、20 bot × 10 分钟浸泡（12,000 ticks 连续）

**门**：logErrors=0、exit 0。30 个 400-tick 窗口全连续。

| 相位 | 10 bot（批次 93） | 20 bot（本轮） | 翻倍增量 |
|---|---|---|---|
| worlds share / 稳态 avg/tick | 36.3-39.2% / 1167us | 35.9-38.5% / 1273us | **+9%** |
| connection share / 稳态 avg/tick | 3.5-5.1% / 120us | 3.4-5.3% / 125us | +4% |
| sendChunks share / 稳态 avg/tick | 0.4-1.5% / 42us | 0.4-1.4% / 45us | +7% |

**扩展性结论**：bot 数翻倍，主线程各相位绝对耗时仅 +4~9%（次线性），主线程仍
~1.3ms/tick（≈2.6% 利用率）——双倍负载增量被离线程管线（区块生成 worker、region IO、
区块发送、存档管线）吸收，主线程串行面不随规模线性放大。相位占比分布与 10 bot 完全
一致，无新热点、无上漂。

## 二、停机竞态扩样（×2，0.59.0 累计 ×7）

两轮全 PASS（exit 0、dat 全合法、重启核验干净）。**本轮第 1 轮在 0.59.0 首次抽样到
上游 POI 内联触发**（upstream-poi-inline=2 = 1 次 occurrence）——与 0.58.1 对照运行的
1 次形成**双版本对称**：该 ERROR 为随机时序事件、与 jar 版本无关，批次 91 的归属链
（上游 `isStopped()` 内联语义 + worker 世界生成 POI 钩子）进一步闭环；门按设计披露
该计数并照常通过。

累计：0.59.0 ×7 轮全 PASS（1 轮含上游 POI 披露）；0.58.1 ×4 轮全 PASS（1 轮含上游
POI 披露，旧口径下另触发 1 次）。

## 三、结论

1. 规模放大（10→20 bot）下主线程各相位次线性增长（+4~9%），多核调度交付的扩展性
   如设计生效；零错误、零上漂、产物合法。
2. 上游 POI 内联 ERROR 双版本对称出现，归属与无害性判定最终闭环。
3. 循环状态：红线内空间判定不变；终止/继续仍待用户决断（本轮为第二次无应答后的
   默认验证轮）。
