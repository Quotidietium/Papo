# 批次 95 报告（2026-08-26）— 0.59.0 随机种子多世界抽样浸泡（回归/验证轮，无服务器代码变更）

主题：去留问询第三次无应答，按默认路径执行**跨种子抽样验证轮**——批次 90/93/94 全部
使用固定种子 papo90（同地形同结构布局）；本轮换种子 `papo95seedB`（20 bot × 10 分钟），
走不同的地形/结构生成路径（生成成本、区块复杂度、POI 分布均随种子变化），检验相位
画像对生成内容的稳健性。benchmark 侧为 TickSurveyBench 增加可选 seed 参数（默认值
不变，保持历史口径可比）。无服务器代码变更，版本保持 0.59.0。

## 一、跨种子浸泡对比（均 20 bot × 600,000ms，12,000 ticks 连续）

| 相位 | papo90（批次 94） | papo95seedB（本轮） | 说明 |
|---|---|---|---|
| worlds share / 稳态 avg/tick | 35.9-38.5% / 1273us | 36.7-39.8% / 1672us | share 一致；绝对值 +31% 为种子地形方差 |
| connection share / 稳态 avg/tick | 3.4-5.3% / 125us | 3.2-4.1% / 156us | 一致 |
| sendChunks share / 稳态 avg/tick | 0.4-1.4% / 45us | 0.4-1.1% / 45us | 逐位一致 |

**门**：logErrors=0、exit 0；30 个 400-tick 窗口全连续、无上漂。

**结论**：相位**占比结构**跨种子稳定（worlds 36-40%、connection 3-5%、sendChunks ~1%）；
绝对耗时随生成内容浮动（worlds +31%）但主线程利用率仍 ~3.3ms/tick 量级（≈3.3%），无
新热点。多核调度交付的画像不是特定地形的产物。

## 二、停机竞态扩样（×2，0.59.0 累计 ×9）

两轮全 PASS（exit 0、dat 全合法、重启核验干净；upstream-poi-inline=0）。
累计：0.59.0 ×9 轮全 PASS（1 轮含上游 POI 披露）。

## 三、结论

1. 跨种子抽样下相位画像稳健，生成内容差异被 worker 池吸收，主线程串行面不变。
2. 累计验证矩阵：长时（93：10bot×10min）+ 规模（94：20bot×10min 次线性）+ 种子
   （95：跨地形稳健）+ 停机竞态（×9）全部全绿——0.59.0 集成态验证充分。
3. 循环状态：红线内空间判定不变；终止/继续仍待用户决断（第三次问询无应答）。
