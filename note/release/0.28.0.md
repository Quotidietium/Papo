# Papo 0.28.0 发布说明

- 基线：Minecraft 1.21.11 / Paper 上游（分支 ver/1.21.11）
- 产物：`paper-server/build/libs/Papo-1.21.11-0.28.0.jar`
- 红线保持：行为等价（默认 true=现状）、API 零变更、游戏版本红线 1.21.11 不变
- **游戏版本红线：始终保持 Minecraft 1.21.11**
- 性质：**安全加固**——补齐 0.27.0 fingerprint-hardening 暂缓的第 4 个开关（V5 命令权限），闭合插件泄露主题

## 新增（直接提交，无补丁）

`paper-global.yml` 的 `fingerprint-hardening` 新增第 4 个开关：

| 开关 | 默认 | 作用 |
|---|---|---|
| `commands.player-visible-defaults` | `true` | `/plugins` `/version` `/help` 的默认权限：`true`（人人，现状）/ `op`（仅 OP）/ `false`（默认无人）|

- **向量 V5**：`bukkit.command.plugins/version/help` 默认权限 TRUE 是唯一**直接吐完整明文插件清单**的向量（主动，但默认 TRUE 使任意改装客户端可触发 `/plugins` 读全量插件列表）。
- **实现**：`GlobalConfiguration.FingerprintHardening.Commands`（新 section）+ `CraftDefaultPermissions.registerCorePermissions`（`CommandPermissions.registerPermissions` 之后按 config 用 `Permission.setDefault` 覆写三者 + recalculate；三者经 `Bukkit.getPluginManager().getPermission` 取得）。
- **时机**：启动时（`enablePlugins`，pluginManager 已就绪、无在线玩家）应用一次；**改 config 需重启生效**（与 brand/status/plugin-channels 三个 live 读取的开关不同）。

详细报告：[note/report/2026-08-02-fingerprint-hardening-commands.md](../report/2026-08-02-fingerprint-hardening-commands.md)。

## 兼容性

- API 零变更；`true`（默认）行为与 0.27.0 逐字一致；config 未加载回退 TRUE。
- `op`/`false` 仅改这三条命令的**默认**权限；OP 经附加/显式赋权不受影响，插件显式赋权不受影响。
- 切 `op` 后普通玩家失去 `/plugins`/`/ver`/`/help`（少数老服依赖需自查）。

## 验证

- compileJava（`--no-configuration-cache`）+ 全量 test BUILD SUCCESSFUL（零 FAILED）。
- 行为自检扩到 **25 项 ALL OK**（新增 commands 的 true/op/false/大小写/空/非法回退 7 项）。
- 性能：启动一次性 setDefault+recalculate，运行时零开销。

## 插件泄露主题闭合

fingerprint-hardening 现覆盖 survey 全部主要向量：
- V1 plugin-channels（被动，0.27.0）
- V2 brand（被动，0.27.0）
- V3a status（被动，0.27.0）
- V5 commands（主动，0.28.0 ← 本版）

V4 Brigadier 命令树复用既有 `spigot.yml` `commands.send-namespaced`，无需新开关。

## 后续（回到性能主线）

- 网络：PacketBundleUnpacker 每包 Consumer、Connection send lambda 池化。
- 聚集：trackedDataValues 刷新延后、ridden getEffectiveRange 缓存。
