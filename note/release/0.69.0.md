# 0.69.0

批次 112（红石轴勘察/基建轮）：

- 0262 探针：`-Dpapo.tickProfile=1` 下 tickPending 段拆分出 blockTicks/fluidTicks/blockEvents 子相位与三类计划 tick 活性计数器（默认关，禁用态零成本）。
- 新增 RedstoneScaleBench 红石负载 harness（中继器环振荡器阵列，天空盒确定性建造，三重在场门）。
- 红石轴画像：计划方块 tick 成本 ~53us/tick 线性；441 环振荡负载 23.7ms/tick 仍保 TPS 20。
- 运营量化：`misc.redstone-implementation: ALTERNATE_CURRENT`（上游既有旋钮）同负载 5.03×——红石重度服可选，默认仍 VANILLA。

服务器行为无变化（探针默认关闭），无 API/配置变更。
