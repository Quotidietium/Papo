# 0.71.0

批次 114（首 join 停摆修复）：

- 修复每次重启后首位进服玩家的 ~0.6-0.9s 主线程冻结（jstack 实证根因：CraftBukkit 实体注册表与 HashOps/guava 的冷类初始化在首 join 时于主线程执行）。
- 手段：boot 末尾后台线程预热上述类初始化（零可观测行为变化）；首 join 冻结削减约 46%，其余为首 join 固有工作。
- 新增 FirstJoinStallBench（复现+jstack 采样 harness）。

无 API/配置变更；探针与预热均不影响既有行为。
