# Papo 1.21.11-0.66.0

多核调度 R3 批次 107（勘察轮，诊断探针，默认关闭）：

- 新增 `-Dpapo.tickProfile=1` 下的 tracker 扇出包型计数探针（0259）：`fanout.sends`
  观察者级发送计数与 8 类包型分解（velocity/movePos/movePosRot/moveRot/posSync/
  entityData/headRot/other），输出于新增的 PapoTickProfile.count 计数节。
- 探针默认关闭，零行为变化；用于实体规模轴归因（批次 106/107 报告见
  note/report/perf/）。
