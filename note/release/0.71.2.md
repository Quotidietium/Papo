# 0.71.2

R3 审计轮修复（批次 119 / 补丁 0267 + PapoDiag 插件修复）：

- join 预取消费有界化：stats/advancements 的登录预取等待从无界 join 改为 60s 有界 + 同步路径回退——极端场景（IO 池病理停滞）下主线程从「永久悬挂」变为有界降级；正常登录零行为差异。
- stats 预取缓存孤儿条目清理：遗留名统计文件回退路径下，每次 join 泄漏一条缓存条目的问题清零。
- PapoDiag 插件：补 `onDisable`——禁用/关服时停掉看门狗线程，消除禁用后 stall-report.txt 每 5 秒一条的伪停摆追加（PapoDiag.jar 已重编，含修复）。

审计闭合面（本轮无缺陷）：0260 发送批量化对抗性边界（FIFO/异常/背压/内存上界/断连）、0264 异步预热竞态、探针族开启态长时安全、异步存档管线（批次79/80/82）链序与崩溃一致性、未信任输入面、配置功能完整性。详见 [审计报告](../report/perf/2026-09-01-r3-audit-batch119.md)。
