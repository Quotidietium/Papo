# 0.73.0（批次 123，2026-09-05）

R4 系列开篇：红石轴 VANILLA 评估级联常量因子优化。

- 红石重负载（环振荡器 N=441）计划方块 tick 相位 **−19.8%**（中位 28.7→23.0 ms/tick），窗方差显著收紧，振荡速率逐位不变
- 方块状态属性读取（全游戏 getValue/setValue 路径）消除每调用哈希探测（直接映射槽缓存）
- 红石粉信号拉取零分配化（每次重算 7~13 个 BlockPos 分配归零）
- 计划 tick 去重检查零分配化（hasScheduledTick/willTickThisTick 探针记录分配归零）
- 每 setBlock 的 POI 变更检查去 Optional 分配
- 新增 tickPending 子相位探针（`-Dpapo.tickProfile=1`，默认关闭零行为）

补丁 0253–0257；全部顺序保持型（不改变任何可观测更新顺序/事件序列/插件 API）。

版本号说明：0.60.0–0.71.4 为已抛弃的 R2+R3 工作段（封存于 tag archive/multicore-r3），
0.72.0 被远端废弃分支占用；为消除歧义本系列自 0.73.0 起。
