# 0.71.4

R3 审计轮修复（批次 122 / 补丁 0269 + PapoDiag 更新）：

- 长期运行内存修复：区块发送缓存（序列化数据/光照快照/压缩共享段）此前在所有玩家离开区块后仍常驻至区块卸载，大规模多玩家服务器上可累积 GB 级占用——现于最后一名观众停止观看时释放，新观众进入时自动重建（字节逐位一致，无行为差异）。
- 诊断插件（PapoDiag）稳健性：停摆报告各分区独立容错（高负载下玩家列表并发读取失败不再丢失主线程栈）；报告文件超过 8MiB 自动轮转（磁盘占用有界）；修复超长时间运行（约 3 年）后心跳计数回绕导致看门狗永久误报的问题。

审计闭合面（本轮无缺陷）：零拷贝帧/发送批量化/flush 缓存叠合出站栈、locale 门控对全部缓存包的完备性、共享区块包快照性与客户端输入清洗、登录链交错与类初始化、白名单包延迟语义（与 vanilla 到达时刻等价）、wire 计量长时安全、探针常量折叠。详见 [审计报告](../report/perf/2026-09-02-crossfamily-audit-batch122.md)。
