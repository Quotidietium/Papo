# Papo 1.21.11-0.68.0

多核调度 R3 批次 111（归因仪器版）：

- ent.serverAiStep 分解探针（0261，默认关，`-Dpapo.tickProfile=1` 启用）：实体链
  容量墙归因的第二刀——Mob AI 计算（goal/nav/look/sensing）与 AI 诱导物理/追踪
  分量切面，配套批次110 NoAI 变体 A/B（容量墙实体链 50/50 分裂：AI 束 11.64ms +
  非 AI 地板 11.41ms @2000 牛）。
- 零行为变化：探针仅计时，不改任何 tick 语义。
