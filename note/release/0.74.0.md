# 0.74.0（批次 125，2026-09-05）

红石粉目标强度计算合并为单遍扫描（每次评估减少约 5 次方块状态读取与 4 次导体重复判定）；新增粉评估冗余勘察探针（`-Dpapo.tickProfile=1` 下 `rs.wireEvalRuns`/`rs.wireEvalNoChange` 计数）。

补丁 0258–0259；百万级随机等价对拍验证；更新顺序与事件序列不变。

同批判例：宏基准对比须附同时窗对照腿（共租环境漂移识别）；珊瑚方块随机流消耗使"形状扇出跳过"类优化不可严格等价，已留档。
