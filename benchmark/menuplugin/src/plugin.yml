name: MenuRefresh
version: 1.0.0
main: papo.menuplugin.MenuRefreshPlugin
api-version: '1.21'
description: Papo batch116 menu-plugin refresh pattern reproduction
commands:
  menurefresh:
    description: set refresh rate Hz (0=off); or "heavy 0|1"
