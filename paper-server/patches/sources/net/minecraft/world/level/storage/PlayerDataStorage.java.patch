--- a/net/minecraft/world/level/storage/PlayerDataStorage.java
+++ b/net/minecraft/world/level/storage/PlayerDataStorage.java
@@ -31,18 +_,31 @@
     }
 
     public void save(Player player) {
+        if (org.spigotmc.SpigotConfig.disablePlayerDataSaving) return; // Spigot
         try (ProblemReporter.ScopedCollector scopedCollector = new ProblemReporter.ScopedCollector(player.problemPath(), LOGGER)) {
             TagValueOutput tagValueOutput = TagValueOutput.createWithContext(scopedCollector, player.registryAccess());
             player.saveWithoutId(tagValueOutput);
             Path path = this.playerDir.toPath();
-            Path path1 = Files.createTempFile(path, player.getStringUUID() + "-", ".dat");
-            CompoundTag compoundTag = tagValueOutput.buildResult();
-            NbtIo.writeCompressed(compoundTag, path1);
+            // Papo start - batch 79: snapshot on main thread, gzip+file IO on IO pool (per-UUID ordered)
+            // The tag tree is built fresh per save, but defensively deep-copy before handing it to
+            // another thread so the delayed write can never observe later main-thread mutations
+            // even if some subsystem ever stored a shared sub-tag.
+            CompoundTag compoundTag = tagValueOutput.buildResult().copy();
             Path path2 = path.resolve(player.getStringUUID() + ".dat");
             Path path3 = path.resolve(player.getStringUUID() + ".dat_old");
-            Util.safeReplaceFile(path2, path1, path3);
+            final String playerName = player.getPlainTextName();
+            io.papermc.paper.util.PapoOrderedFileWrites.enqueue(path2, () -> {
+                try {
+                    Path path1 = Files.createTempFile(path, player.getStringUUID() + "-", ".dat");
+                    NbtIo.writeCompressed(compoundTag, path1);
+                    Util.safeReplaceFile(path2, path1, path3);
+                } catch (Exception var11) {
+                    LOGGER.warn("Failed to save player data for {}", playerName, var11);
+                }
+            });
+            // Papo end - batch 79: snapshot on main thread, gzip+file IO on IO pool (per-UUID ordered)
         } catch (Exception var11) {
-            LOGGER.warn("Failed to save player data for {}", player.getPlainTextName());
+            LOGGER.warn("Failed to save player data for {}", player.getPlainTextName(), var11); // Paper - Print exception
         }
     }
 
@@ -61,10 +_,33 @@
     }
 
     private Optional<CompoundTag> load(NameAndId nameAndId, String suffix) {
+        // Papo start - batch 79: a quick re-join can race the async .dat write; wait for it so
+        // the read always observes the latest save, exactly like the synchronous code did.
+        if (suffix.equals(".dat")) {
+            io.papermc.paper.util.PapoOrderedFileWrites.awaitPending(
+                this.playerDir.toPath().resolve(nameAndId.id().toString() + ".dat"));
+        }
+        // Papo end - batch 79: read-after-write visibility for the async .dat write
         File file = new File(this.playerDir, nameAndId.id() + suffix);
+        // Spigot start
+        boolean usingWrongFile = false;
+        if (org.bukkit.Bukkit.getOnlineMode() && !file.exists()) { // Paper - Check online mode first
+            file = new File(this.playerDir, net.minecraft.core.UUIDUtil.createOfflinePlayerUUID(nameAndId.name()) + suffix);
+            if (file.exists()) {
+                usingWrongFile = true;
+                LOGGER.warn("Using offline mode UUID file for player {} as it is the only copy we can find.", nameAndId.name());
+            }
+        }
+        // Spigot end
         if (file.exists() && file.isFile()) {
             try {
-                return Optional.of(NbtIo.readCompressed(file.toPath(), NbtAccounter.unlimitedHeap()));
+                // Spigot start
+                Optional<CompoundTag> optional = Optional.of(NbtIo.readCompressed(file.toPath(), NbtAccounter.unlimitedHeap()));
+                if (usingWrongFile) {
+                    file.renameTo(new File(file.getPath() + ".offline-read"));
+                }
+                return optional;
+                // Spigot end
             } catch (Exception var5) {
                 LOGGER.warn("Failed to load player data for {}", nameAndId.name());
             }
@@ -84,4 +_,10 @@
             return DataFixTypes.PLAYER.updateToCurrentVersion(this.fixerUpper, compoundTag, dataVersion);
         });
     }
+
+    // CraftBukkit start
+    public File getPlayerDir() {
+        return this.playerDir;
+    }
+    // CraftBukkit end
 }
