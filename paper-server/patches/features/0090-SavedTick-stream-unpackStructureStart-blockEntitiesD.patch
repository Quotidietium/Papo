From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Zurker <91011862+hershate@users.noreply.github.com>
Date: Thu, 30 Jul 2026 20:56:24 +0800
Subject: [PATCH] SavedTick stream unpackStructureStart blockEntitiesD


diff --git a/net/minecraft/network/protocol/game/ClientboundLevelChunkPacketData.java b/net/minecraft/network/protocol/game/ClientboundLevelChunkPacketData.java
index 1bef34100ec5c6b717878e0eb566671ba4b56a77..1c3557072345944acf6aab4c1476f5d5c038999c 100644
--- a/net/minecraft/network/protocol/game/ClientboundLevelChunkPacketData.java
+++ b/net/minecraft/network/protocol/game/ClientboundLevelChunkPacketData.java
@@ -65,7 +65,7 @@ public class ClientboundLevelChunkPacketData {
             chunkPacketInfo.setBuffer(this.buffer);
         }
         extractChunkData(new FriendlyByteBuf(this.getWriteBuffer()), levelChunk, chunkPacketInfo);
-        this.blockEntitiesData = Lists.newArrayList();
+        this.blockEntitiesData = Lists.newArrayListWithExpectedSize(levelChunk.getBlockEntities().size()); // Papo - presize (only appended here, only iterated later)
         int totalTileEntities = 0; // Paper - Handle oversized block entities in chunks
 
         for (Entry<BlockPos, BlockEntity> entry : levelChunk.getBlockEntities().entrySet()) {
diff --git a/net/minecraft/world/level/chunk/storage/SerializableChunkData.java b/net/minecraft/world/level/chunk/storage/SerializableChunkData.java
index 95c8d65a4b6db8d0721460ae55cb8fb8143ea9a2..c7cc66faf668a06527557a75e432a6e3e498903f 100644
--- a/net/minecraft/world/level/chunk/storage/SerializableChunkData.java
+++ b/net/minecraft/world/level/chunk/storage/SerializableChunkData.java
@@ -185,8 +185,22 @@ public record SerializableChunkData(
                 }
             }
 
-            List<CompoundTag> list3 = tag.getList("entities").stream().flatMap(ListTag::compoundStream).toList();
-            List<CompoundTag> list4 = tag.getList("block_entities").stream().flatMap(ListTag::compoundStream).toList();
+            // Papo start - plain loops instead of Optional.stream().flatMap(compoundStream).toList()
+            // (per chunk load, x2). getList() empty <=> getListOrEmpty() empty list; compoundStream keeps
+            // only CompoundTag elements; consumers only iterate the results.
+            List<CompoundTag> list3 = new ArrayList<>();
+            for (net.minecraft.nbt.Tag entityTag : tag.getListOrEmpty("entities")) {
+                if (entityTag instanceof CompoundTag entityCompound) {
+                    list3.add(entityCompound);
+                }
+            }
+            List<CompoundTag> list4 = new ArrayList<>();
+            for (net.minecraft.nbt.Tag blockEntityTag : tag.getListOrEmpty("block_entities")) {
+                if (blockEntityTag instanceof CompoundTag blockEntityCompound) {
+                    list4.add(blockEntityCompound);
+                }
+            }
+            // Papo end
             CompoundTag compoundOrEmpty = tag.getCompoundOrEmpty("structures");
             ListTag listOrEmpty1 = tag.getListOrEmpty("sections");
             List<SerializableChunkData.SectionData> list5 = new ArrayList<>(listOrEmpty1.size());
@@ -729,10 +743,12 @@ public record SerializableChunkData(
             if (structure == null) {
                 LOGGER.error("Unknown structure start: {}", identifier);
             } else {
-                StructureStart structureStart = StructureStart.loadStaticStart(context, compoundOrEmpty.getCompoundOrEmpty(string), seed);
+                // Papo - hoist the repeated getCompoundOrEmpty(string) (double hash lookup + Optional alloc)
+                CompoundTag startTag = compoundOrEmpty.getCompoundOrEmpty(string);
+                StructureStart structureStart = StructureStart.loadStaticStart(context, startTag, seed);
                 if (structureStart != null) {
                     // CraftBukkit start - load persistent data for structure start
-                    net.minecraft.nbt.Tag persistentBase = compoundOrEmpty.getCompoundOrEmpty(string).get("StructureBukkitValues");
+                    net.minecraft.nbt.Tag persistentBase = startTag.get("StructureBukkitValues");
                     if (persistentBase instanceof CompoundTag compoundTag) {
                         structureStart.persistentDataContainer.putAll(compoundTag);
                     }
diff --git a/net/minecraft/world/ticks/SavedTick.java b/net/minecraft/world/ticks/SavedTick.java
index 30a4dff64943898303ac7087cb9960f10d0a2fd2..f9f68b4107d6f8a84d32f28125ae3ae844a2677f 100644
--- a/net/minecraft/world/ticks/SavedTick.java
+++ b/net/minecraft/world/ticks/SavedTick.java
@@ -43,7 +43,19 @@ public record SavedTick<T>(T type, BlockPos pos, int delay, TickPriority priorit
 
     public static <T> List<SavedTick<T>> filterTickListForChunk(List<SavedTick<T>> tickList, ChunkPos chunkPos) {
         long packedChunkPos = chunkPos.toLong();
-        return tickList.stream().filter(savedTick -> ChunkPos.asLong(savedTick.pos()) == packedChunkPos).toList();
+        // Papo start - plain loop instead of stream().filter().toList() (per chunk load, x2 for block/fluid
+        // ticks). Consumers only iterate the result.
+        if (tickList.isEmpty()) {
+            return List.of();
+        }
+        List<SavedTick<T>> result = new java.util.ArrayList<>(tickList.size());
+        for (SavedTick<T> savedTick : tickList) {
+            if (ChunkPos.asLong(savedTick.pos()) == packedChunkPos) {
+                result.add(savedTick);
+            }
+        }
+        return result;
+        // Papo end
     }
 
     public ScheduledTick<T> unpack(long gameTime, long subTickOrder) {
