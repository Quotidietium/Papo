From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Zurker <91011862+hershate@users.noreply.github.com>
Date: Fri, 31 Jul 2026 20:08:56 +0800
Subject: [PATCH] WalkNodeEvaluator.getPathType scratch MutableBlockPo


diff --git a/net/minecraft/world/level/pathfinder/FlyNodeEvaluator.java b/net/minecraft/world/level/pathfinder/FlyNodeEvaluator.java
index 19fd3d4e80f1cbc72f76760d68344df4a3ca887e..1c0c23ca7bcd2d6906bf0e141ed5019dc146f762 100644
--- a/net/minecraft/world/level/pathfinder/FlyNodeEvaluator.java
+++ b/net/minecraft/world/level/pathfinder/FlyNodeEvaluator.java
@@ -289,8 +289,12 @@ public class FlyNodeEvaluator extends WalkNodeEvaluator {
     public PathType getPathType(PathfindingContext context, int x, int y, int z) {
         PathType pathTypeFromState = context.getPathTypeFromState(x, y, z);
         if (pathTypeFromState == PathType.OPEN && y >= context.level().getMinY() + 1) {
-            BlockPos blockPos = new BlockPos(x, y - 1, z);
-            PathType pathTypeFromState1 = context.getPathTypeFromState(blockPos.getX(), blockPos.getY(), blockPos.getZ());
+            // Papo start - avoid BlockPos allocation: the pos below would be (x, y-1, z) by construction
+            // and is only decomposed back to coordinates plus one BlockPos.equals against mobPosition()
+            // (Vec3i.equals compares x/y/z; a fresh pos is never reference-equal to the stored mob pos),
+            // so compare coordinates directly instead of materializing the BlockPos.
+            PathType pathTypeFromState1 = context.getPathTypeFromState(x, y - 1, z);
+            // Papo end
             if (pathTypeFromState1 == PathType.DAMAGE_FIRE || pathTypeFromState1 == PathType.LAVA) {
                 pathTypeFromState = PathType.DAMAGE_FIRE;
             } else if (pathTypeFromState1 == PathType.DAMAGE_OTHER) {
@@ -298,7 +302,9 @@ public class FlyNodeEvaluator extends WalkNodeEvaluator {
             } else if (pathTypeFromState1 == PathType.COCOA) {
                 pathTypeFromState = PathType.COCOA;
             } else if (pathTypeFromState1 == PathType.FENCE) {
-                if (!blockPos.equals(context.mobPosition())) {
+                // Papo - coordinate compare replacing blockPos.equals(context.mobPosition()) (see above)
+                BlockPos papoMobPos = context.mobPosition();
+                if (!(x == papoMobPos.getX() && y - 1 == papoMobPos.getY() && z == papoMobPos.getZ())) {
                     pathTypeFromState = PathType.FENCE;
                 }
             } else {
diff --git a/net/minecraft/world/level/pathfinder/WalkNodeEvaluator.java b/net/minecraft/world/level/pathfinder/WalkNodeEvaluator.java
index 28ec361a41bbccd30c67ba81a53b92eac36abfb4..5ba4cb9d53c3dbd7220044db298e64650170fb90 100644
--- a/net/minecraft/world/level/pathfinder/WalkNodeEvaluator.java
+++ b/net/minecraft/world/level/pathfinder/WalkNodeEvaluator.java
@@ -38,6 +38,10 @@ public class WalkNodeEvaluator extends NodeEvaluator {
     // for-each) in exact Plane.HORIZONTAL order, and reuse a mutable pos for getFloorLevel.
     private static final Direction[] HORIZONTAL = {Direction.NORTH, Direction.EAST, Direction.SOUTH, Direction.WEST};
     private final BlockPos.MutableBlockPos floorLevelPos = new BlockPos.MutableBlockPos();
+    // Scratch pos for getPathType: getPathTypeStatic(PathfindingContext, MutableBlockPos) reads the
+    // coordinates into local ints at entry and never retains or mutates the pos, so a per-evaluator
+    // reusable instance is safe (pathfinding runs on the tick thread, evaluators are per-navigation).
+    private final BlockPos.MutableBlockPos papoPathTypePos = new BlockPos.MutableBlockPos();
     // Papo end
 
     @Override
@@ -454,7 +458,9 @@ public class WalkNodeEvaluator extends NodeEvaluator {
 
     @Override
     public PathType getPathType(PathfindingContext context, int x, int y, int z) {
-        return getPathTypeStatic(context, new BlockPos.MutableBlockPos(x, y, z));
+        // Papo - reuse scratch pos instead of allocating a MutableBlockPos per call (see field comment);
+        // subclasses override this method wholesale and do not call super, so the field is Walk-only.
+        return getPathTypeStatic(context, this.papoPathTypePos.set(x, y, z));
     }
 
     public static PathType getPathTypeStatic(Mob mob, BlockPos pos) {
