From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Zurker <91011862+hershate@users.noreply.github.com>
Date: Sat, 1 Aug 2026 02:53:40 +0800
Subject: [PATCH] Papo pos tick tick 1 MutableBlockPos


diff --git a/net/minecraft/world/level/block/entity/BeaconBlockEntity.java b/net/minecraft/world/level/block/entity/BeaconBlockEntity.java
index cea93d8add8fb618be03f171a3b293bcceca809e..02e706e5f626a65241db4355237270af0102bd83 100644
--- a/net/minecraft/world/level/block/entity/BeaconBlockEntity.java
+++ b/net/minecraft/world/level/block/entity/BeaconBlockEntity.java
@@ -161,11 +161,16 @@ public class BeaconBlockEntity extends BlockEntity implements MenuProvider, Name
         super(BlockEntityType.BEACON, pos, blockState);
     }
 
+    // Papo start - cross-tick scratch pos for the beam scan (0160): extends the earlier loop-level
+    // reuse; the pos never escapes (getBlockState reads coordinates, getY/setY only). Main thread only.
+    private final BlockPos.MutableBlockPos papoBeamScanPos = new BlockPos.MutableBlockPos();
+    // Papo end
+
     public static void tick(Level level, BlockPos pos, BlockState state, BeaconBlockEntity blockEntity) {
         int x = pos.getX();
         int y = pos.getY();
         int z = pos.getZ();
-        BlockPos.MutableBlockPos blockPos = new BlockPos.MutableBlockPos(); // Papo - reuse a mutable pos for the beam scan (was new BlockPos + .above() per iteration)
+        BlockPos.MutableBlockPos blockPos = blockEntity.papoBeamScanPos; // Papo - cross-tick scratch (0160, was new MutableBlockPos per tick)
         if (blockEntity.lastCheckY < y) {
             blockPos.set(pos);
             blockEntity.checkingBeamSections = Lists.newArrayList();
