From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Zurker <91011862+hershate@users.noreply.github.com>
Date: Sat, 1 Aug 2026 04:28:51 +0800
Subject: [PATCH] =?UTF-8?q?=E5=BC=B9=E5=B0=84=E7=89=A9=E5=80=99=E9=80=89?=
 =?UTF-8?q?=E5=BE=AA=E7=8E=AF=E9=9B=B6=E5=8D=8A=E5=BE=84=20inflate=20?=
 =?UTF-8?q?=E5=88=86=E9=85=8D=E8=B7=B3=E8=BF=87=E4=B8=A4=E5=A4=84=EF=BC=88?=
 =?UTF-8?q?0169=EF=BC=89?=
MIME-Version: 1.0
Content-Type: text/plain; charset=UTF-8
Content-Transfer-Encoding: 8bit


diff --git a/net/minecraft/world/entity/projectile/ProjectileUtil.java b/net/minecraft/world/entity/projectile/ProjectileUtil.java
index 230366938b70e2210c2b6ebb3678164b485ebc61..5063b0a74c109c987112db383d05dd5db561575a 100644
--- a/net/minecraft/world/entity/projectile/ProjectileUtil.java
+++ b/net/minecraft/world/entity/projectile/ProjectileUtil.java
@@ -126,7 +126,11 @@ public final class ProjectileUtil {
         Vec3 vec3 = null;
 
         for (Entity entity1 : level.getEntities(source, boundingBox, filter)) {
-            AABB aabb = entity1.getBoundingBox().inflate(entity1.getPickRadius());
+            // Papo - skip the inflate allocation when the pick radius is zero: inflate(0) is value-identical
+            // for entity bounding boxes (min-0.0==min even for -0.0; max+0.0==max because makeBoundingBox
+            // maxes are pos + nonneg half-width and therefore can never be -0.0), and clip/contains only read.
+            float papoPickRadius = entity1.getPickRadius();
+            AABB aabb = papoPickRadius == 0.0F ? entity1.getBoundingBox() : entity1.getBoundingBox().inflate(papoPickRadius);
             Optional<Vec3> optional = aabb.clip(startVec, endVec);
             if (aabb.contains(startVec)) {
                 if (d >= 0.0) {
@@ -173,7 +177,8 @@ public final class ProjectileUtil {
         Entity entity = null;
 
         for (Entity entity1 : level.getEntities(source, boundingBox, filter)) {
-            AABB aabb = entity1.getBoundingBox().inflate(margin);
+            // Papo - same zero-inflate skip as above (margin >= 0 via computeMargin clamp, never -0.0)
+            AABB aabb = margin == 0.0F ? entity1.getBoundingBox() : entity1.getBoundingBox().inflate(margin);
             Optional<Vec3> optional1 = aabb.clip(startVec, endVec);
             if (optional1.isPresent()) {
                 double d1 = startVec.distanceToSqr(optional1.get());
