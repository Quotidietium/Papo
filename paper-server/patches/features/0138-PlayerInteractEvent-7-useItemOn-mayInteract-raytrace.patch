From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Zurker <91011862+hershate@users.noreply.github.com>
Date: Fri, 31 Jul 2026 23:21:14 +0800
Subject: [PATCH] PlayerInteractEvent 7 useItemOn mayInteract raytrace


diff --git a/net/minecraft/server/level/ServerPlayerGameMode.java b/net/minecraft/server/level/ServerPlayerGameMode.java
index 3466634e4f2b51dc646ec47f546132b6827baac0..12845b9fdb9d5431d2c182290cb61a290b40e206 100644
--- a/net/minecraft/server/level/ServerPlayerGameMode.java
+++ b/net/minecraft/server/level/ServerPlayerGameMode.java
@@ -186,7 +186,10 @@ public class ServerPlayerGameMode {
             if (action == ServerboundPlayerActionPacket.Action.START_DESTROY_BLOCK) {
                 if (!this.level.mayInteract(this.player, pos)) {
                     // CraftBukkit start - fire PlayerInteractEvent
-                    org.bukkit.craftbukkit.event.CraftEventFactory.callPlayerInteractEvent(this.player, org.bukkit.event.block.Action.LEFT_CLICK_BLOCK, pos, face, this.player.getInventory().getSelectedItem(), InteractionHand.MAIN_HAND);
+                    // Papo - zero-listener: fire-and-forget call, skip entirely
+                    if (org.bukkit.event.player.PlayerInteractEvent.getHandlerList().getRegisteredListeners().length > 0) {
+                        org.bukkit.craftbukkit.event.CraftEventFactory.callPlayerInteractEvent(this.player, org.bukkit.event.block.Action.LEFT_CLICK_BLOCK, pos, face, this.player.getInventory().getSelectedItem(), InteractionHand.MAIN_HAND);
+                    }
                     this.player.connection.send(new ClientboundBlockUpdatePacket(pos, this.level.getBlockState(pos)));
                     this.debugLogging(pos, false, sequence, "may not interact");
                     this.capturedBlockEntity = true; // Paper - Send block entities after destroy prediction
@@ -195,11 +198,27 @@ public class ServerPlayerGameMode {
                 }
 
                 // CraftBukkit start
-                org.bukkit.event.player.PlayerInteractEvent event = org.bukkit.craftbukkit.event.CraftEventFactory.callPlayerInteractEvent(this.player, org.bukkit.event.block.Action.LEFT_CLICK_BLOCK, pos, face, this.player.getInventory().getSelectedItem(), InteractionHand.MAIN_HAND);
-                if (event.isCancelled()) {
+                // Papo start - zero-listener fast path: statically replicate the event outcomes
+                // (6-arg call: cancelledBlock=false, pos non-null => useInteractedBlock=ALLOW =>
+                // isCancelled() false; cancelledItem=false => useItemInHand=DEFAULT)
+                final boolean papoInteractCancelled;
+                final boolean papoUseInteractedBlockDeny;
+                final boolean papoUseItemInHandDeny;
+                if (org.bukkit.event.player.PlayerInteractEvent.getHandlerList().getRegisteredListeners().length > 0) {
+                    org.bukkit.event.player.PlayerInteractEvent event = org.bukkit.craftbukkit.event.CraftEventFactory.callPlayerInteractEvent(this.player, org.bukkit.event.block.Action.LEFT_CLICK_BLOCK, pos, face, this.player.getInventory().getSelectedItem(), InteractionHand.MAIN_HAND);
+                    papoInteractCancelled = event.isCancelled();
+                    papoUseInteractedBlockDeny = event.useInteractedBlock() == org.bukkit.event.Event.Result.DENY;
+                    papoUseItemInHandDeny = event.useItemInHand() == org.bukkit.event.Event.Result.DENY;
+                } else {
+                    papoInteractCancelled = false;
+                    papoUseInteractedBlockDeny = false;
+                    papoUseItemInHandDeny = false;
+                }
+                if (papoInteractCancelled) { // Papo (was: event.isCancelled())
                     this.capturedBlockEntity = true; // Paper - Send block entities after destroy prediction
                     return;
                 }
+                // Papo end
                 // CraftBukkit end
 
                 if (this.player.getAbilities().instabuild) {
@@ -223,7 +242,7 @@ public class ServerPlayerGameMode {
                 this.destroyProgressStart = this.gameTicks;
                 float f = 1.0F;
                 BlockState blockState = this.level.getBlockState(pos);
-                if (event.useInteractedBlock() != org.bukkit.event.Event.Result.DENY && !blockState.isAir()) { // Paper
+                if (!papoUseInteractedBlockDeny && !blockState.isAir()) { // Papo (was: event.useInteractedBlock() != DENY) // Paper
                     EnchantmentHelper.onHitBlock(
                         this.level,
                         this.player.getMainHandItem(),
@@ -240,7 +259,7 @@ public class ServerPlayerGameMode {
 
                 // CraftBukkit start
                 // Note that we don't need to resync blocks, block acks will handle it properly for everything but block entities already
-                if (event.useItemInHand() == org.bukkit.event.Event.Result.DENY) {
+                if (papoUseItemInHandDeny) { // Papo (was: event.useItemInHand() == DENY)
                     return;
                 }
 
@@ -504,14 +523,30 @@ public class ServerPlayerGameMode {
         if (player.getCooldowns().isOnCooldown(stack)) {
             cancelledItem = true; // Paper - correctly handle items on cooldown
         }
-        org.bukkit.event.player.PlayerInteractEvent event = org.bukkit.craftbukkit.event.CraftEventFactory.callPlayerInteractEvent(player, org.bukkit.event.block.Action.RIGHT_CLICK_BLOCK, blockPos, hitResult.getDirection(), stack, cancelledBlock, cancelledItem, hand, hitResult.getLocation()); // Paper - correctly handle items on cooldown
+        // Papo start - zero-listener fast path: statically replicate the event outcomes
+        // (pos non-null => useInteractedBlock = cancelledBlock ? DENY : ALLOW;
+        // useItemInHand = cancelledItem ? DENY : DEFAULT; constructor defaults verified in paper-api)
+        final boolean papoUseInteractedBlockDeny;
+        final boolean papoUseItemInHandDeny;
+        final boolean papoUseItemInHandAllow;
+        if (org.bukkit.event.player.PlayerInteractEvent.getHandlerList().getRegisteredListeners().length > 0) {
+            org.bukkit.event.player.PlayerInteractEvent event = org.bukkit.craftbukkit.event.CraftEventFactory.callPlayerInteractEvent(player, org.bukkit.event.block.Action.RIGHT_CLICK_BLOCK, blockPos, hitResult.getDirection(), stack, cancelledBlock, cancelledItem, hand, hitResult.getLocation()); // Paper - correctly handle items on cooldown
+            papoUseInteractedBlockDeny = event.useInteractedBlock() == org.bukkit.event.Event.Result.DENY;
+            papoUseItemInHandDeny = event.useItemInHand() == org.bukkit.event.Event.Result.DENY;
+            papoUseItemInHandAllow = event.useItemInHand() == org.bukkit.event.Event.Result.ALLOW;
+        } else {
+            papoUseInteractedBlockDeny = cancelledBlock;
+            papoUseItemInHandDeny = cancelledItem;
+            papoUseItemInHandAllow = false;
+        }
+        // Papo end
         this.firedInteract = true;
-        this.interactResult = event.useItemInHand() == org.bukkit.event.Event.Result.DENY;
+        this.interactResult = papoUseItemInHandDeny; // Papo (was: event.useItemInHand() == DENY)
         this.interactPosition = blockPos.immutable();
         this.interactHand = hand;
         this.interactItemStack = stack.copy();
 
-        if (event.useInteractedBlock() == org.bukkit.event.Event.Result.DENY) {
+        if (papoUseInteractedBlockDeny) { // Papo (was: event.useInteractedBlock() == DENY)
             // Block acks will take care of most of it, just handle some special cases here
             if (blockState.getBlock() instanceof net.minecraft.world.level.block.CakeBlock) {
                 player.getBukkitEntity().sendHealthUpdate(); // SPIGOT-1341 - reset health for cake
@@ -526,7 +561,7 @@ public class ServerPlayerGameMode {
             }
             // Paper end - Fix inventory desync
             this.player.resyncUsingItem(this.player); // Paper - Properly cancel usable items
-            return (event.useItemInHand() != org.bukkit.event.Event.Result.ALLOW) ? InteractionResult.SUCCESS : InteractionResult.PASS;
+            return (!papoUseItemInHandAllow) ? InteractionResult.SUCCESS : InteractionResult.PASS; // Papo (was: event.useItemInHand() != ALLOW)
         } else if (this.gameModeForPlayer == GameType.SPECTATOR) {
             MenuProvider menuProvider = blockState.getMenuProvider(level, blockPos);
             if (menuProvider != null && player.openMenu(menuProvider).isPresent()) { // Paper - Fix InventoryOpenEvent cancellation
diff --git a/net/minecraft/server/network/ServerGamePacketListenerImpl.java b/net/minecraft/server/network/ServerGamePacketListenerImpl.java
index 14cbca7d6a52a58fd96be59f48bd2e56a31e5183..5a0eb641a27952509a917cdcd35cc4c7ca897907 100644
--- a/net/minecraft/server/network/ServerGamePacketListenerImpl.java
+++ b/net/minecraft/server/network/ServerGamePacketListenerImpl.java
@@ -2195,14 +2195,24 @@ public class ServerGamePacketListenerImpl
 
                 boolean cancelled;
                 if (hitResult == null || hitResult.getType() != HitResult.Type.BLOCK) {
-                    org.bukkit.event.player.PlayerInteractEvent event = CraftEventFactory.callPlayerInteractEvent(this.player, Action.RIGHT_CLICK_AIR, itemInHand, hand);
-                    cancelled = event.useItemInHand() == Event.Result.DENY;
+                    // Papo - zero-listener: useItemInHand defaults to DEFAULT (never DENY) => cancelled = false
+                    if (org.bukkit.event.player.PlayerInteractEvent.getHandlerList().getRegisteredListeners().length > 0) {
+                        org.bukkit.event.player.PlayerInteractEvent event = CraftEventFactory.callPlayerInteractEvent(this.player, Action.RIGHT_CLICK_AIR, itemInHand, hand);
+                        cancelled = event.useItemInHand() == Event.Result.DENY;
+                    } else {
+                        cancelled = false;
+                    }
                 } else {
                     if (this.player.gameMode.firedInteract && this.player.gameMode.interactPosition.equals(hitResult.getBlockPos()) && this.player.gameMode.interactHand == hand && ItemStack.isSameItemSameComponents(this.player.gameMode.interactItemStack, itemInHand)) {
                         cancelled = this.player.gameMode.interactResult;
                     } else {
-                        org.bukkit.event.player.PlayerInteractEvent event = CraftEventFactory.callPlayerInteractEvent(this.player, Action.RIGHT_CLICK_BLOCK, hitResult.getBlockPos(), hitResult.getDirection(), itemInHand, true, hand, hitResult.getLocation());
-                        cancelled = event.useItemInHand() == Event.Result.DENY;
+                        // Papo - zero-listener: useItemInHand defaults to DEFAULT (cancelledBlock only sets useInteractedBlock) => cancelled = false
+                        if (org.bukkit.event.player.PlayerInteractEvent.getHandlerList().getRegisteredListeners().length > 0) {
+                            org.bukkit.event.player.PlayerInteractEvent event = CraftEventFactory.callPlayerInteractEvent(this.player, Action.RIGHT_CLICK_BLOCK, hitResult.getBlockPos(), hitResult.getDirection(), itemInHand, true, hand, hitResult.getLocation());
+                            cancelled = event.useItemInHand() == Event.Result.DENY;
+                        } else {
+                            cancelled = false;
+                        }
                     }
                     this.player.gameMode.firedInteract = false;
                 }
@@ -2665,6 +2675,9 @@ public class ServerGamePacketListenerImpl
         if (this.player.isImmobile()) return; // CraftBukkit
         this.player.resetLastActionTime();
         // CraftBukkit start - Raytrace to look for 'rogue armswings'
+        // Papo start - zero listeners: the origin Location, the rayTrace (a pure query) and the three
+        // fire-and-forget PlayerInteractEvent calls exist solely for this event => skip the whole block
+        if (org.bukkit.event.player.PlayerInteractEvent.getHandlerList().getRegisteredListeners().length > 0) {
         float f1 = this.player.getXRot();
         float f2 = this.player.getYRot();
         double d0 = this.player.getX();
@@ -2689,6 +2702,8 @@ public class ServerGamePacketListenerImpl
                 CraftEventFactory.callPlayerInteractEvent(this.player, Action.LEFT_CLICK_AIR, this.player.getInventory().getSelectedItem(), InteractionHand.MAIN_HAND);
             }
         } // Paper end - Call interact event
+        }
+        // Papo end
 
         // Arm swing animation
         io.papermc.paper.event.player.PlayerArmSwingEvent event = new io.papermc.paper.event.player.PlayerArmSwingEvent(this.getCraftPlayer(), org.bukkit.craftbukkit.CraftEquipmentSlot.getHand(packet.getHand())); // Paper - Add PlayerArmSwingEvent
